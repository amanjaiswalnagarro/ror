
<nav class="navbar">

  <div class="navbar-menu">
    <div class="navbar-start">
    <div class="navbar-item">
    <%= link_to "Blogs", root_path, class: "button is-primary" %>
    </div>
    </div>
    <div class="navbar-end">
      <% if user_signed_in? %>
        <div class="navbar-item">
          <%= link_to "Edit Profile", edit_user_registration_path, class: "button is-primary" %>
        </div>
        <div class="navbar-item">
          <%= form_with(url: destroy_user_session_path, method: :delete) do |form| %>
            <%= form.submit "Logout" , class:"button is-danger" %>
          <% end %>
          
        </div>
      <% else %>
        <div class="navbar-item">
          <%= link_to "Sign In", new_user_session_path, class: "button is-primary" %>
        </div>
        <div class="navbar-item">
          <%= link_to "Sign Up", new_user_registration_path, class: "button is-info" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
